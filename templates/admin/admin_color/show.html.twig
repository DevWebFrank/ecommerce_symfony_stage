{% extends 'admin/base.html.twig' %}

{% block title %}Color
{% endblock %}

{% block body %}
	<div class="container my-3">
		<div class="row">
			<div class="col-8">
<h1>Couleur
	{{ color.name }}</h1>


				<table class="table">
					<tbody>
						<tr>
							<th>Id</th>
							<td>{{ color.id }}</td>
						</tr>
						<tr>
							<th>Name</th>
							<td>{{ color.name }}</td>
						</tr>
					</tbody>
				</table>

				<a class="textDecoration-none" href="{{ path('admin/admin_color_index') }}">Retour à la liste</a>

				<a href="{{ path('admin/admin_color_edit', {'id': color.id}) }}">éditer</a>

				{{ include('admin/admin_color/_delete_form.html.twig') }}
			</div>
		</div>
	</div>


{% endblock %}
