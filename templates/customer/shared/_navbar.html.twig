<!-- Header -->
<nav class="navbar navbar-expand-lg navbar-light shadow colorBgNav">

	<div class="container d-flex justify-content-between align-items-center">

		<a class="navbar-brand logo h1 align-self-center colorLogo" href="{{path("home")}}">FFR</a>

		<button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#templatemo_main_nav" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		{# DEBUT NAV ORIGINAL #}
		<div class="align-self-center collapse navbar-collapse flex-fill  d-lg-flex justify-content-lg-between" id="templatemo_main_nav">

			<ul class="nav navbar-nav d-flex justify-content-between mx-lg-auto">

				<ul class="navbar-nav me-auto mb-2 mb-lg-0">

					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Categories
						</a>
						<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
							{% for category in categoryRepository.findAll() %}
								<li>
									<a class="dropdown-item" href=" {{ path('boutique_product_show_by_category',{'id':category.id} ) }}">
										{{category.name}}</a>
								</li>
								<li><hr class="dropdown-divider"></li>
							{% endfor %}
						</ul>
					</li>
				</ul>

				{% if app.user %}

					{% if app.user.roles[0] is same as "ROLE_ADMIN" %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path("admin_home")}}">Administration</a>
						</li>
					{% endif %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path("app_logout")}}">Se deconnecter</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path("app_login")}}">Se connecter</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_register')}}">S'inscrire</a>
					</li>
				{% endif %}

			</ul>
			
			{# NAV BLOG/CART/PROFIL #}
			<div class="navbar align-self-center d-flex">

				<a class="nav-link" aria-current='page' href="{{ path('blog_post_list') }}">Blog</a>


				<a class="nav-icon d-none d-lg-inline" href="#" data-bs-toggle="modal" data-bs-target="#templatemo_search">
					<i class="fa fa-fw fa-search text-dark mr-2"></i>
				</a>

				<a class="nav-icon position-relative text-decoration-none" href="{{ path('cart_detail') }}">
					<i class="fa fa-fw fa-cart-arrow-down text-dark mr-1"></i>
					<span class="position-absolute top-0 left-100 translate-middle badge rounded-pill bg-light text-dark">
						{{ handleCart.detailPanier() | length }}
					</span>
				</a>
				<a class="nav-icon position-relative text-decoration-none" href="{{ path('profile_detail') }}">

					<i class="fa fa-fw fa-user text-dark mr-3"></i>
					<!-- <span class="position-absolute top-0 left-100 translate-middle badge rounded-pill bg-light text-dark">+99</span> -->
				</a>
			</div>
			{# FIN NAV BLOG/CART/PROFIL #}
		</div>
		{# FIN NAV ORIGINAL #}
	</div>{# FIN DE LA DIV CONTAINER #}</nav>{# FIN DE LA NAV #}
<!-- Close Header -->
